version: "3.9"

services:
  nextjs:
    container_name: lahzenama
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    restart: always
    environment:
      NODE_ENV: production
    networks:
      - lahzenama

networks:
  lahzenama:
    external: true
